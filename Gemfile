source 'https://rubygems.org'

gem 'rails', '4.2.5'
gem 'sass-rails', '~> 5.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.1.0'
gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0', group: :doc


group :development, :test do
	gem 'sqlite3'
	gem 'byebug'
	gem 'web-console', '~> 2.0'
	gem 'annotate', '~> 2.7' # setup using rails g annotate:install
	# https://github.com/danielpclark/PolyBelongsTo
	gem 'poly_belongs_to', '~> 0.2.8'
	gem 'coffee-script-source', '1.8.0'
end

group :test do

end

group :production do

end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]